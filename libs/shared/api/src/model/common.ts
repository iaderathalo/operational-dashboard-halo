/**
 * Make the common interfaces available from a single module.
 *
 * While there is currently only one import, this file allows for
 * addition items to be added to common.
 */

import ErrorResponse from './common/ErrorResponse';

export default ErrorResponse;
