# Decision Log — PRD: Operational Dashboard (Phase 1)

- 2026-06-13 — Run created. Intent: **create**. Requested by Iader. Scope deliberately limited to
  **Phase 1** (Datadog health integration, app-service-ID mapping, BU-tree reorg,
  perception-scenario discovery) — the work achievable while creator Anton is OOO, for a strong
  first impression on return.
- 2026-06-13 — Working mode: **Fast path**, autonomous (Iader's directive: maximize progress, don't
  wait). Inferences tagged `[ASSUMPTION]` inline and indexed in §9 for later review rather than
  blocking on questions.
- 2026-06-13 — `document-project` (brownfield codebase docs) deliberately SKIPPED before this PRD —
  grounding is already strong (repo recon intent dossier + two authoritative transcripts + market
  research). Backfill only if a gap surfaces.
- 2026-06-13 — Inputs (all in `_bmad-output/planning-artifacts/`): finalized brief + addendum,
  creator intent dossier, market research report, cleaned FW touch-point transcript. PRD assembled
  from these (already authored/read this session — no extraction subagents needed).
- 2026-06-13 — Parallel hardening workflow (wf_cddb87ca-98c, Sonnet): Stage 1 = validate PRD
  (7-dimension rubric) ‖ Crawler solution-design; Stage 2 = epics & stories. Rationale for NOT doing
  all 3 in parallel: stories depend on validation+design (BMAD order), so only the
  mutually-independent validate+design were parallelized.
- 2026-06-13 — Validation result: grade **Good** (Decision-readiness/Substance/Strategic/Scope/Shape
  = strong; Done-ness + Downstream usability = adequate). 10 findings (3 high, 4 medium, 3 low). ALL
  high + medium + actionable low applied to prd.md: (HIGH) FR-4 uptime windows enumerated
  (24h/7d/30d/90d) + 90d-vs-retention caveat; FR-2 No-Data→AMBER inlined and distinguished from
  Unmapped (FR-10); new §G Data Contract naming Crawler write/read fields (the Application model had
  no health fields). (MED) FR-13 IT-owner context mechanism; FR-12 LOB-table storage; SM-1 in-scope
  app-list note; Open Qs 9/10/11 added. (LOW) FR-9↔FR-7 resolution-path cross-ref; §B
  prototype-file correction (portfolio.html/detail.html, not demo.html). review-rubric.md saved to
  workspace. PRD → status: final.
- 2026-06-13 — Artifacts saved: Crawler solution-design →
  planning-artifacts/architecture/crawler-solution-design-2026-06-13.md; Phase-1 backlog (4 epics,
  15 stories) → planning-artifacts/epics-stories/phase-1-backlog-2026-06-13.md. Backlog is
  repo-grounded (real file paths) with a thin-vertical-slice sequence E1-S1→S3→S4→S5 to "first app
  Health live"; first verifiable milestone = curl the internal sync endpoint. Build can start with
  mocked Datadog before credentials arrive (E1-S7/E2-S1 blocked on real Service IDs + Datadog key
  from Prashant/Nemi). NOTE: Bernardo is the build lead per the meeting; Iader shadows → ownership,
  so how far to auto-build is a decision for Iader. — Cross-Cutting NFRs, Integration &
  Dependencies, Operational Requirements, Constraints & Guardrails, Risks & Mitigations,
  Stakeholders. Dropped consumer/monetization/hardware/public-API clusters (not applicable).
  Technical "how" (Datadog API shape, perception formula template) lives in the brief's addendum and
  the PRD addendum, not the PRD body.
- 2026-06-16 — **In-place reconciliation of prd.md against a working, live-tested implementation + 5
  meeting transcripts.** Intent: **update** (not rewrite); structure and untouched sections
  preserved; superseded text struck through / annotated rather than deleted to keep the audit trail;
  frontmatter `updated` bumped 2026-06-13 → 2026-06-16. Requested by Iader. Reconciliations applied:
  - **People/naming corrected:** owner is **Anand** Novikov (auto-captions garble to Anton/Anthony),
    owns Mercer Intellify; added **Tanu** (IntelliFi perception specifics) alongside Raja; Rami
    reframed as senior stakeholder whose ask is "group by **LOB, not TPM**"; Iader = dev taking
    ownership; Bernardo decides key cards. (§F, §3, §4.4)
  - **(a) Resolved open questions.** Q3 _app↔service-ID mapping_ → RESOLVED as an **automatic
    dual-tag bridge**, NOT manual curation (was `[ASSUMPTION]` in FR-8). Q2 _number-of-active-users_
    → RESOLVED **OUT** (no Datadog metric; Anand: replace the card). Q1/§4.4 _perception_ →
    **mechanism DEFINED**, thresholds/values still TBD with **Raja + Tanu**. Q10 _LOB table storage_
    → MOOT (no table; structured fields). Struck-through in §8 with resolutions; §9 assumptions for
    FR-8/FR-12 marked SUPERSEDED.
  - **(b) Datadog integration → bulk-fetch snapshot crawler.** §4.1 Description + FR-1 rewritten
    from per-app polling to one snapshot/run (monitors paged once, SLOs paged once keeping
    app_short_key/app_service_id-tagged, each kept SLO's 24h/7d/30d history under bounded
    concurrency 6 + 429 Retry-After backoff), then local resolve with zero per-app Datadog calls.
    Rationale recorded: per-app issued ~3656+ calls/run, tripped 429 (live 13 failures, 521/651);
    snapshot re-run 3656/3656, 0 errors, 651 mapped, ~157s. Endpoints + CronJob trigger added to
    §B/§C; rate-limit + write-path NFRs to §A.
  - **(c) App-mapping requirement → automatic app_short_key/app_service_id bridge.** §4.2 fully
    reworked: `app_short_key`==`castKey`/`shortCode` (primary; optional `datadogServiceId`
    override), `app_service_id`==`serviceNowKey` (fallback); validated by the 104-namespace ×
    all-id-field brute-force probe (only these two are per-app bridges;
    `business_unit`/`team`/`service`/`servicenow_chg` are group-level/coincidental and excluded).
    FR-8/FR-9/FR-10 rewritten; UJ-2 corrected (Intellify HAS monitors under app_short_key — live
    GREEN 99.99%; Beacon genuinely has none). Glossary terms added (app_short_key, app_service_id),
    Unmapped redefined.
  - **(d) Coverage reality added.** 651/3656 monitored (GREEN 561, AMBER 49, RED 41; ~190 SLO-backed
    uptime); the ~3000 unmapped simply lack monitors; **monitor creation = owning app team's/SRE's
    job, not the dashboard team's**. New "Coverage reality" block in §4.2; folded into SM-2, §6.2
    (out of scope), and §E (two new rows; old Service-ID-blank risk row marked RESOLVED).
  - **(e) Portfolio tree → OpCo → BU → LOB from PlanView structured fields.** §4.3 retitled and
    rewritten; FR-11 root = Operating Company (Mercer/Marsh/Oliver Wyman/Guy Carpenter/CIS/MMC),
    then BU then LOB; FR-12 = derived from PlanView `OpCo`/`BusinessDeliveryPortfolioName`, NOT a
    hardcoded taxonomy or regex bucketing (earlier single-"US Consulting" bucket + regex spike =
    throwaways). Glossary OpCo/BU/LOB added; SM-3 updated; PlanView loader note (preserves
    structured fields) in §B.
  - **(f) User-Perception mechanism defined (still discovery of values).** §4.4 adds the "bank
    analogy" mechanism: per app pick 2–4 real operations → backing API → baseline → response-time
    threshold → ok/critical, independent of Health; dedicated tab after Overview+Health showing
    operation/current/baseline/ok-vs-critical. FR-14 extended (backing API per entry); owners Raja +
    Tanu.
  - **Also captured:** card-flexibility direction + Phase-1 priority/**delivered** (Overview+Health
    from real Datadog for Fiber/Beacon/VIP/IntelliFi) in §6.1; hardening (timing-safe token compare,
    non-array body throws, unknown-state→AMBER fail-safe, error-budget target<100 guard,
    healthStatus gated on datadogMapped, `$set` never clobbers name/tier/statusOverride) across
    FR-2/FR-5/FR-7/§A/§G; §G data contract reworked to the real fields
    (shortCode/serviceNowKey/datadogServiceId/datadogMapped/uptime30d/...).
    **Dev-only/not-for-commit items deliberately NOT documented as product**; only
    SYNC_APP_SHORTCODES/SYNC_CONCURRENCY recorded as legitimate ops config.
- 2026-06-16 — **Second 2026-06-16 reconciliation pass: branch/commit state + Raja sync + Item I
  enrichment roadmap.** Intent: **update** (additive only; prior entries untouched; no FR
  renumbering; existing reconciliation preserved). Requested by Iader. Added to prd.md:
  - **Branch/commit state.** §4.1 now records the build/branch state: the sync logic + internal
    token-guarded endpoint `POST /api/v1/internal/sync/datadog` live in `apps/api` and are
    DONE/validated, committed on branch **`feature/datadog-live-health`** (commit **`fc8f6da`**),
    excluding `apps/crawler` + local-dev-only files. The `apps/crawler` K8s CronJob (auto-trigger)
    is **BUILT but HELD OUT** of the commit pending **Bernie's** validation that a CronJob is the
    right approach.
  - **(N3) Live-vs-placeholder is per COLUMN, not per app.** New block in §6.1: only **Health** +
    **Uptime** are LIVE from Datadog (mapped apps) in `toPortfolioApp`
    (`apps/api/src/dashboard/mongo/mongo-portfolio.repository.ts`); **Perception/Active
    Users/Incidents/Last Incident** are HARDCODED placeholders; **Total Internal/External Users**
    are real-but-PlanView. DIRECTIVE: replace dummy cards with real Datadog data where available,
    drop/replace what we cannot source; **Perception re-confirmed OUT**. Near-term honest move =
    legend + grayed-out (`.metric-muted`) placeholders + "live · Datadog · synced N min ago"
    provenance tooltip, driven by the already-present
    `datadogMapped`/`resolutionPath`/`lastSyncStatus`/`lastSyncAt`.
  - **(N1/N2/N5) IntelliFi-first demo + live re-confirm.** §6.1 records Anand's (via Raja) immediate
    task: focus on **ONE good-data app — IntelliFi — as the demoable proof**; live demo validated
    end-to-end with Raja (Health+Uptime live on IntelliFy + HVCAP; ~3 apps in Anand's portfolio
    return data); the `app_short_key` bridge re-confirmed live (ServiceNow key often null →
    `app_service_id` is fallback).
  - **(N4) Beacon monitoring EXISTS — supersede "Beacon has no monitors."** UJ-2 de-Beaconized (now
    a generic unmonitored-app example); §4.2 probe note annotated; Glossary `app_service_id` example
    fixed; **new Open Q12** records Beacon monitoring DOES exist and the remaining work is
    **coordinating with Juan** for Beacon's `app_short_key`/mapping (not creating monitors). **Juan
    added to §F stakeholders.**
  - **(N7) Deployment dependency.** §B + **new Open Q13**: Raja needs a **deployed (non-localhost)**
    demo link for the Anand meeting; deploy = `repository_dispatch` "devops-trigger" into the
    **Polaris/MMC unified pipeline** (api/ui images via Helm to AWS). Iader pushes
    `feature/datadog-live-health`, runs the deploy **with Bernie** (owns the know-how), tags
    Raja/Prashant. **Risk:** Iader has not deployed Polaris before → real dependency on Bernie.
  - **(Item I) Engineering enrichment roadmap recorded in BMAD.** New **§8.1**: priority-ordered
    roadmap from the untracked repo-root `DATADOG-NEXT-STEPS.md` — (1) health-timeline
    endpoint+render (FR-3, half-built; `health_snapshots` already written), (2) monitor drill-down
    (`group_states=all`), (3) Downtimes API (suppress false RED), (4) derived maturity SCORECARD,
    (5) Service Catalog ⚠️ (`service` tag is NOT a clean per-app join here — probe first), (6)
    Incidents API ⚠️ (needs Incident Management product). Plus the **user-perception technical
    path** (still discovery, OUT of Phase-1 build): 3 RUM-free sources (APM `trace.*` / log-based
    distribution metric / multistep API Synthetic test) → one baseline engine (`calendar_shift(-7d)`
    ratio + `anomalies('agile',2)`); constraints metrics 15-month retention, `/api/v1/query` ~1600
    req/h/org. Tied back to N3: the dummy cards stop being placeholders as #6 (Incidents) and the
    perception source land.
- 2026-06-16 — **CORRECTION (naming): Anton ≠ Anand.** The earlier reconciliation above
  ("People/naming corrected: owner is **Anand** Novikov, auto-captions garble to Anton/Anthony") was
  **wrong** — it merged two distinct people. Confirmed by Iader 2026-06-16: **Anton Novikov** is the
  creator/owner/TPM (on vacation; owns Mercer Intellify + the portfolio; approves on return), while
  **Anand** is a _separate_ senior stakeholder the demo is being prepared for (gave the immediate
  IntelliFi task via Raja). Corroboration: the Raja transcript distinguishes "**Anton** portfolio"
  (only 3 apps return data) from "the immediate task we got from **Anand**". Applied to `prd.md` §F
  (split into two stakeholder entries), "POCs while Anand is out" → "while **Anton** is out", and
  "~3 apps in **Anand's** portfolio" → "**Anton's** portfolio". Other "Anand" references (the Anand
  demo / meeting / immediate task) are correct and left as-is. Prior log entries left intact
  (append-only).
