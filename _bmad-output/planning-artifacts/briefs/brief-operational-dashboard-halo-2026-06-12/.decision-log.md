# Decision Log — Product Brief: Operational Dashboard

- 2026-06-12 — Run created. Intent: **create**. Requested by Iader (dev on team), who asked for the
  brief to be produced from the FW_dashboard meeting transcript using the BMAD workflow.
- 2026-06-12 — Source material: the FW_dashboard meeting transcript found heavily corrupted (2,588
  lines, only 32 unique — transcription glitch repeats two sentences thousands of times). A
  deduplicated extract was used instead. The call audio is also **truncated**: it cuts off right as
  Prashant asks about Datadog integration.
- 2026-06-12 — Facts recovered from transcript: Polaris-template project; MEAN-style stack with
  Mongo Atlas; deployed to development region via unified pipeline; current data is dummy data
  served from Mongo; main goal = health status for applications in the US Health portfolio; primary
  audience = TPMs and business team; open thread on prod/non-prod Okta groups for the unified
  pipeline (DevOps ticket vs developer portal — parked for end of call); participants Anton
  (presenter/owner), Prashant, Bernardo, Yadar/Iader.
- 2026-06-12 — Iader redirected the flow: before drafting, run a repo-reconnaissance workflow whose
  primary goal is extracting the creator's (Anton's) intent from the code — the transcript alone is
  too thin/corrupted. Sequence: (1) repo recon → intent dossier, (2) web research on repo findings
  (all agents on Sonnet per Iader's instruction), (3) then update/draft the product brief from both.
  Workflow run ID: wf_55ec18dc-461.
- 2026-06-12 — User instruction mid-run: ALL workflow agents on Sonnet (not just research). First
  run (wf_01... predecessor) stopped; relaunched as wf_01e97364-567 with model: sonnet everywhere.
  Completed: 13 agents, 6 research topics. One recon agent (infra) lost to an API socket error — its
  ground was partially covered by docs-meta/api readers and prior team knowledge; flagged as a
  coverage gap, not silently dropped.
- 2026-06-12 — Workflow outputs saved: creator intent dossier →
  `_bmad-output/planning-artifacts/research/creator-intent-dossier-2026-06-12.md`; market/product
  research report →
  `_bmad-output/planning-artifacts/research/market-operational-dashboard-research-2026-06-12.md`.
- 2026-06-12 — Brief drafted (status: draft) from dossier + research. Key drafting decisions: (a)
  added a non-template "Creator Intent — said vs built vs prepared" section because the brief's
  purpose is partly to capture Anton's mental model while he's away; (b) positioned product as
  business-stakeholder health layer, NOT an IDP (research: the "why not Backstage/Port?" challenge
  is preempted by the persona gap); (c) User Perception named primary differentiator (no
  off-the-shelf equivalent found); (d) AI Tokens/AI Drift tabs explicitly flagged as forward scope,
  not live (PolarisMetadata declares AIintegration: false); (e) success criterion added that
  Perception must never show an ungrounded color; (f) recommended V1 perception = Apdex-only from
  existing Datadog APM (research-backed, zero new instrumentation) — pending Anton's formula
  decision; (g) deep detail (prereqs checklist, formula template, risk register, contradictions,
  two-vendor AI architecture) routed to addendum.md, keeping the brief ~2 pages.
- 2026-06-12 — Pending for finalize: doc-standards polish passes (bmad-editorial-review-structure,
  bmad-editorial-review-prose), decision-log audit with Iader, and Iader's review of the draft. Open
  questions assigned: perception formula / dataset reconciliation / AI tab scope → Anton (returns
  week of 2026-06-22); Datadog org topology → checkable by team now.
- 2026-06-12 — Supplementary context pulled from session memory (verified 2026-06-11/12): two
  dimensions per app (Health = Datadog telemetry; User Perception = business-operation timings, the
  differentiator per sponsor Rami Assaad); portfolio apps Mercer Intellify, Beacon, FIBER, VIP;
  phase 1 = Datadog, phase 2 = Komodor; Pendo for page-load/user counts; ingestion via K8s CronJob;
  perception formula still undefined (Anton to supply business metrics after vacation); CI name
  "PORTFOLIO VISIBILITY DASHBOARD", app key `operdas1`; repo currently all hardcoded seed data (no
  Datadog client, no Planview code yet).
- 2026-06-13 — Iader supplied a clean SRT transcript (the corrupted txt is superseded):
  `FW Operational Dashboard Touch Point.srt` (the authoritative meeting, fixed). Cleaned text saved
  to `_bmad-output/planning-artifacts/transcripts/`. These resolve the earlier validation gap — the
  "[CONFIRMAR]" items are now evidence-backed, so no confirm tags were needed.
- 2026-06-13 — Validation outcome against the authoritative meeting: CONFIRMED — User Perception =
  timing of business scenarios vs baseline (Anton's own examples: IntelliFi report gen 8s→47s,
  census upload; Beacon "save session" >1min); Perception is THE differentiator (Anton + sponsor
  Rami); Komodor = phase 2 explicitly; Datadog = immediate next step; ingestion = K8s cronjob ("the
  crawler"), NOT in-app cron because replicas cause concurrent duplicate writes; app↔Datadog
  mapping via application-service-ID per app in Mongo (blank in PlanView, manual; namespace+app-name
  fallback); 2-week Datadog log retention; no RUM license. CORRECTED — the business-unit tree reorg
  was requested by **Rami** (sponsor), NOT Jory; Jory is Anton's **supervisor**. Earlier "Jorie
  Blackwell / Delivery Lead" attribution was wrong (fixed in brief + project memory). NEW people:
  Nemi (dev, point of contact), Raja (business contact for perception scenarios). RECONCILED — Anton
  demoed a demo/real data toggle + PlanView load + IT-owner filtering from UNCOMMITTED local
  changes, which explains their absence from the single-commit repo snapshot; the flat
  `applications` collection = real PlanView export, the portfolio tree = demo data (two MODES, not
  two permanent disjoint datasets — substantially resolves former open question on dataset
  reconciliation).
- 2026-06-13 — Brief FINALIZED (status: draft → final), fully re-grounded in the transcripts.
  Editorial standards (structure: pyramid/MECE/evidence-supports; prose: Microsoft style, minimal,
  no hedging) applied during authoring. Addendum expanded with an "Authoritative meeting evidence"
  section (people, perception scenarios, the crawler + concurrency rationale, mapping challenge,
  real/demo toggle, access/provisioning state, Pendo-vs-Datadog-logs debate). Per Iader's directive
  ("don't wait for Anton, maximize progress for a good first impression"), proceeding to next BMAD
  steps: document-project → Phase-1 PRD. Remaining open questions are genuine unknowns (perception
  formula specifics, number-of-users mechanism, app-service-ID values, AI-tab scope), carried in the
  brief — not finalize blockers.
- 2026-06-16 — As-built reconciliation against validated live reality. **SUPERSEDES** the mapping
  clause in the 2026-06-13 validation entry above — specifically "app↔Datadog mapping via
  application-service-ID per app in Mongo (blank in PlanView, manual; namespace+app-name fallback)".
  That model is **dead**. Prior entries are left intact (append-only log); this entry is the current
  source of truth. Changes:
  - **Mapping = automatic tag bridge.** The per-app identifier is Datadog tag `app_short_key` ==
    PlanView CAST key (== app shortCode), **primary**; fallback is tag `app_service_id` == PlanView
    ServiceNow key (`SNSVC#######`). An exhaustive coverage probe proved these are the ONLY reliable
    per-app bridges (`service`/`business_unit`/`team`/`servicenow_chg` are not). No service-ID is
    blank-in-PlanView, curated by an operator, or set per app; there is no namespace+app-name /
    `kube_namespace` / `kube_app_name` fallback. Re-confirmed live in the Raja demo: `app_short_key`
    works; the ServiceNow key is often null (hence `app_service_id` is only the fallback).
  - **Ingestion = bulk-fetch snapshot.** Each sync pages ALL monitors + ALL relevant SLOs once and
    resolves per-kept-SLO history under bounded concurrency (6) with 429 backoff; per-app resolution
    is then purely local (zero Datadog HTTP in the per-app loop). This replaced the earlier per-app
    fetch loop that tripped Datadog 429 rate limits. Live result: **651 apps mapped, 0 errors,
    ~157s, over 3656 apps** (not "12 seed apps"). Health rollup = monitor `overall_state`
    worst-state-wins; unmapped → AMBER (never a false green); a `statusOverride` always wins.
  - **Portfolio tree = PlanView OpCo → BusinessDeliveryPortfolioName** (Business Unit → LOB); the
    old owner/person grouping and the "LOB-to-BU normalization mapping table" idea are both killed.
  - **Crawler split / held out.** Sync logic + internal token-guarded endpoint
    (`POST /api/v1/internal/sync/datadog`) live in `apps/api` and are done/validated. The separate
    `apps/crawler` K8s CronJob (auto-trigger) is built but **held out** of the commit/branch pending
    **Bernie's** validation that a CronJob is the right approach. Committed on branch
    `feature/datadog-live-health` (commit `fc8f6da`), excluding `apps/crawler` + local-dev-only
    files.
  - **Raja sync (meet Raja–Saule–Iader, 2026-06-16).** (1) Pipeline validated live end-to-end —
    Health + Uptime live from Datadog for mapped apps (confirmed on IntelliFy and HVCAP; ~3 apps in
    Anton's portfolio return data); was dummy before, now sourced from Datadog. (2) **Perception is
    OUT of the Phase-1 build (re-confirmed).** (3) **Replace the dummy Overview cards** with real
    Datadog-sourced data where available; only Health + Uptime are live, the other ~7 cards are
    dummy; drop ones we cannot source (e.g. total external users). (4) Immediate task (from Anand,
    via Raja): focus on **IntelliFy** as the demoable proof for the Anand meeting. (5) **Deploy a
    demoable link** (not localhost): Iader coordinates with Bernie for the Polaris / MMC
    unified-pipeline deploy (`repository_dispatch` "devops-trigger"; api/ui images via Helm to AWS),
    push the branch, tag Raja/Prashant — Iader has not done a Polaris deploy before
    (dependency/risk).
  - **Beacon monitoring EXISTS** — supersedes the earlier "Beacon genuinely has no monitors / its
    team must create them" assumption; coordinate with **Juan** (owns Beacon monitoring) for
    Beacon's `app_short_key` / mapping.
  - **Per-column provenance.** In `toPortfolioApp`
    (`apps/api/src/dashboard/mongo/mongo-portfolio.repository.ts`): Health and Uptime are LIVE from
    Datadog; Perception, Active Users, Incidents, Last Incident are hardcoded placeholders; Total
    Internal/External Users come from PlanView (real). Model already carries `datadogMapped` /
    `resolutionPath` / `lastSyncStatus` / `lastSyncAt` to drive a provenance UI.
  - **Enrichment roadmap recorded.** Prioritized "what else to show" (health-timeline
    endpoint+render FR-3, monitor drill-down, Downtimes API, maturity scorecard, Incidents API,
    Service Catalog) + the still-discovery user-perception technical path currently live in
    repo-root `DATADOG-NEXT-STEPS.md`; to be folded into the PRD.
